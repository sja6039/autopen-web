# URL of the Raspberry Pi running the Autopen HMI server.
# No trailing slash. This is never sent to the browser.
AUTOPEN_HMI_URL=http://192.168.1.50:5000

# Port for the backend server (Vite dev server runs on 5173 separately).
PORT=3001
